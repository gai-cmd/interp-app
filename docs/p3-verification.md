# P3 검증 기준과 원장

기준: [design-p3.md](design-p3.md) §1·§2(오케스트레이터 필수 실검증)·§4, [DESIGN.md](DESIGN.md) §9, [design-v0.6.md](design-v0.6.md) §17·§19, [release-checklist.md](release-checklist.md). 이 문서는 P3-01이 만들고 P3-12·17·38·39가 과제 ID 절을 **추가**한다. 이전 절의 미검증 표시는 지우지 않는다.

## 1. 판정 원칙

- **자동 검사 통과 ≠ 수동 완료.** G 세 명령과 `node --test tests/`는 구현 회귀의 증거다. 화면 배치·실기기·실키·배포·규모 판정은 §3의 수동 항목에 실제 증거(환경·릴리스·일시·담당·관측)가 붙어야 바뀐다.
- 증거가 없는 칸은 `미검증`으로 둔다. 미측정을 성공·비해당·0으로 바꾸지 않는다. 실패는 지우지 않고 재시험 회차를 잇는다.
- Node 테스트의 모의 DOM·CSS 문자열 검사는 실제 레이아웃·대비·터치 영역 실측을 대신하지 않는다. `_headers` 검사 통과는 GitHub Pages의 실제 응답 헤더 적용 증거가 아니다.
- 허브 서버가 `policy.control` 확장을 보내기 전에는 허브 실시간 통제를 fixture 검증까지만 완료로 표시한다.
- P2에서 보류된 항목(§4)은 P3 자동 검사가 아무리 통과해도 보류 상태를 유지한다. 해제는 해당 P2 문서의 증거표에 실측이 채워질 때만 가능하다.
- 키·방 코드·QR payload·인증 URL·발화 원문·원본 오디오는 증거에 넣지 않는다. 비밀을 제거한 집계와 참조 ID만 남긴다.

## 2. 자동 검사

| 검사 | 명령 | 범위 |
|---|---|---|
| 전체 회귀 | `node --test tests/*.test.mjs` | 모든 과제 |
| 디렉터리 진입 | `node --test tests/` | 모든 과제(P1-20b 이후 `tests/package.json` 덕분에 동작) |
| 사전 완전성 | `node scripts/check-i18n.mjs` | 세 언어 키 집합·placeholder·사용 키·동적 열거 키(P3-32부터 `admin/*.html`·`app/admin/` 포함) |
| 공백 | `git diff --check` | 모든 과제 |
| 릴리스 포장 | `node scripts/stage-release.mjs --id <id> --out <임시경로>` → `node scripts/check-release.mjs <임시경로>` | P3-13·35·36 및 정책·관리자·SW 변경 과제. 산출물은 검사 후 삭제 |
| 과제별 지정 검사 | design-p3.md §2의 각 과제 "완료 확인 명령" | 해당 과제 |

각 과제 보고서는 위 명령을 **직접 실행한 출력**(통과 수, 실패·취소·skip·todo 0, I18N_OK의 키 수)을 적는다. 기존 테스트 기대값을 바꿨으면 파일·단언·이유를 적는다. 삭제·skip으로 우회하지 않는다.

### P3-01 로컬 실행 결과 (2026-09-06, Node v24.18.0, HEAD `b20575c` + 문서 3개 변경)

| 명령 | 결과 |
|---|---|
| `node --test tests/*.test.mjs` | 607 통과, 실패·취소·skip·todo 0 |
| `node --test tests/` | 내부 606개 + 디렉터리 진입 1개 통과 |
| `node scripts/check-i18n.mjs` | I18N_OK, languages=3 keys=321 files=57 |
| `git diff --check` | 통과 |

P3-01은 코드를 바꾸지 않았으므로 위 수치는 P3-00 직후 기준선과 같다. 이 결과는 문서 완료 확인이며 §3·§4의 어떤 수동 항목도 해제하지 않는다.

## 3. 수동 검증 항목

오케스트레이터(또는 오너)가 실제 브라우저·기기·배포에서 확인한다. 각 행은 환경·릴리스 ID·일시·담당·관측을 붙여 판정을 바꾼다. 담당 과제는 구현 완료 시 해당 행에 "구현 완료·미검증"을 적을 수 있고, 검증 완료는 실측 증거가 있을 때만 적는다.

### 3.1 반응형·표시 (design-p3 §1.9·§1.10, DESIGN.md §9)

| ID | 확인 | 방법 | 담당 과제 | 판정 |
|---|---|---|---|---|
| V01 | 430px·768px·1280px에서 가로 스크롤 없음, 본문 최대 폭(40rem·60rem), 순차 데스크톱 좌 24rem·우 가변 2열 | 실제 브라우저 창 크기 변경·스크린샷 | P3-12·17 | P3-12 부분(본문 최대 폭 40rem·60rem, min-width 브레이크포인트) 구현 완료·미검증. P3-17 부분(순차 데스크톱 좌 24rem·우 가변 2열 `.seq[data-layout="desktop"]`, 말풍선 `overflow-wrap: anywhere`) 구현 완료·미검증  **P3-41: 부분 확인 — headless Chrome 렌더 실측(p3-40-20260907):** 세 폭 가로 스크롤 없음(scrollWidth == innerWidth). 최대 폭·열 치수는 미검증 (§P3-41). |
| V02 | 모든 버튼·입력 44px 이상, PTT 모바일 64px·데스크톱 56px, S 글자에서도 유지 | 개발자 도구 크기 측정 | P3-12·17 | P3-12 부분(`--touch: 44px` 고정 px, PTT `max(64px, 4rem)`/`max(56px, 3.5rem)`) 구현 완료·미검증. P3-17 부분(PTT 전폭 단독 행, 시작/종료·취소 행 `flex: 1 1 8rem`, 텍스트 입력 `min-height: var(--touch)`) 구현 완료·미검증  **P3-41: 부분 확인 — headless Chrome 렌더 실측(p3-40-20260907):** PTT 364×64 / 598×64 / 384×56 확인. 가시 조작 요소 중 키 배지만 65×28로 실패, 나머지 44px 이상. 배지 수정 후 실측 및 S 글자는 미검증 (§P3-41). |
| V03 | 네 톤 × 라이트/다크에서 본문·악센트 위 글자 대비 4.5:1 이상 | 대비 도구 실측(자동 계산 검사는 P3-12) | P3-12 | 구현 완료·자동 계산 통과(아래 P3-12 절)·실측 미검증 |
| V04 | 시스템 모드 변경 즉시 반영, 강제 라이트가 시스템 다크 위에서 유지 | OS 설정 전환 | P3-14 | 미검증  **P3-41: 부분 확인 — headless Chrome 렌더 실측(p3-40-20260907):** 다크 표시 확인. OS 변경 즉시 반영·강제 라이트는 미검증 (§P3-41). |
| V05 | XL 글자·일본어·200% 확대에서 헤더 줄바꿈, 언어 토글이 overflow 메뉴에 숨지 않음 | 브라우저 확대 | P3-12·15 | P3-12 부분(`html[data-text]` 4단계, 헤더 `flex-wrap`) 구현 완료·미검증. 언어 토글 배치는 P3-15  **P3-41: 부분 확인 — headless Chrome 렌더 실측(p3-40-20260907):** 430px XL·일본어 헤더·컨트롤·키 안내 줄바꿈 확인. 200% 확대는 미검증 (§P3-41). |
| V06 | 저장된 표시 설정이 첫 페인트 전 적용(깜빡임 없음), 저장소 차단 시 system/navy/m으로 부팅 | 새로고침 반복 관찰·프라이빗 창 | P3-13 | 미검증 |
| V07 | 포커스 링 2px·offset 2px 가시, Tab 순서 = 시각 순서, reduced-motion에서 전환 0 | 키보드만으로 전체 흐름 | P3-12·16·17 | P3-12 부분(포커스 링·reduced-motion) 구현 완료·미검증. P3-17 부분(순차 화면 Tab 순서 = DOM 순서 = 시각 순서, 두 배치 모두 CSS `order` 없이 실제 노드 이동, 전환 시 포커스 유지) 구현 완료·미검증. 시트 Tab 순서는 P3-16  **P3-41: 부분 확인 — headless Chrome 렌더 실측(p3-40-20260907):** 탭·닫기의 포커스 링 가시 확인. 전체 Tab 순서·reduced-motion 실기기는 미검증 (§P3-41). |
| V08 | 설정·화면·공유 시트가 상호 배타적으로 열리고, inert·Escape·포커스 복귀·내용 스크롤·sticky 행동 영역 동작 | 모바일 시트·데스크톱 모달 각각 | P3-16 | 미검증 |
| V09 | 동시통역 자막판 가−/가+ 1~2rem, 고대비 옵션, 글자 변경 시 스크롤 위치 유지 | 실제 자막 수신 중 조작 | P3-18 | 미검증  **P3-41: 부분 확인 — headless Chrome 렌더 실측(p3-40-20260907):** 가−/가+·고대비 표시 확인. 실제 자막 수신·스크롤 보존은 미검증 (§P3-41). |
| V10 | P2-25 공유 다이얼로그: 열기·닫기·URL 텍스트·복사·포커스 복귀·`role=dialog` 스크린리더 읽기 | 실제 브라우저·스크린리더 | P3-15·16 | 미검증 (P2-25 인계) |

### 3.2 다국어·키 안내·설정 (design-p3 §1.11·§1.12)

| ID | 확인 | 방법 | 담당 과제 | 판정 |
|---|---|---|---|---|
| V11 | 첫 실행이 기기 언어(`navigator.languages`)를 따르고 선택이 저장·복원되며 통역 언어·활성 연결은 유지 | 새 프로필·언어 전환 중 통역 | P3-15 | 미검증 |
| V12 | 언어 전환 시 문서 `lang`·제목·manifest 링크 갱신 | 개발자 도구 | P3-15 | 미검증 |
| V13 | 세 언어 전체 문구의 자연스러움·용어·존댓말 검토 | 언어별 통독(정적 검사 아님) | P3-37 | 미검증 |
| V14 | 키 안내 카드가 설정·첫 실행·키 없는 직접 통역 빈 화면에 나오고 허브 청취 빈 자막판에는 없음, 링크가 새 탭·`noopener noreferrer` | 화면 확인 | P3-21 | 미검증 |
| V15 | 개인 키 표시/숨김 토글, 저장 후 입력란 비움·숨김 복귀, 저장 키 자동 재노출 없음, 저장 실패 시 성공 문구 없음 | 실기기 확인(키 값 화면은 녹화 금지) | P3-22 | 미검증  **P3-41: 부분 확인 — headless Chrome 렌더 실측(p3-40-20260907):** 34개 마스킹 문자·표시/숨김 토글·저장 문구 확인. 저장 실패·자동 재노출 방지 실기기 시험은 미검증 (§P3-41). |
| V16 | 설정 섹션 순서·설명 문구, 기존 진단·PWA 버튼·공용 종료가 재배치 후에도 동작 | 화면 확인 | P3-19 | 미검증 |

### 3.3 마이크 권한·오디오 장치 (design-p3 §1.14)

| ID | 확인 | 방법 | 담당 과제 | 판정 |
|---|---|---|---|---|
| V17 | iOS Safari·Android Chrome·데스크톱 각각 권한 거부 → 안내 → 재허용 흐름, 안내의 메뉴 문구가 실제 버전과 일치 | 실기기 | P3-24 | 미검증 |
| V18 | 설정 저장 시 임시 트랙 즉시 종료(표시등 꺼짐), 시작 시 스트림 인계로 이중 요청 없음, 헤더·허브·관리자에서 요청 0회 | 실기기 표시등·개발자 도구 | P3-24 | 미검증 |
| V19 | 권한 후 장치 라벨 표시, `devicechange`·전경 복귀 갱신, 장치 소실 시 기본값 복귀 | 실제 이어폰 연결/해제 | P3-25·28 | 미검증  **P3-41: 부분 확인 — headless Chrome 렌더 실측(p3-40-20260907):** 미요청 권한 상태·장치 select 표시만 확인. 권한 후 라벨·장치 변경·소실 복귀는 미검증 (§P3-41). |
| V20 | 입력 장치 변경이 현재 녹음·직접 Live를 종료하고 다음 수동 시작에 적용 | 실기기 | P3-26 | 미검증 |
| V21 | `AudioContext.setSinkId` 지원 브라우저에서 PCM 출력 변경, 미지원에서는 노출 없음, 기기 음성은 시스템 출력 안내 | 지원 브라우저·미지원 브라우저 각각 | P3-27·28 | 미검증 |

### 3.4 정책·관리자·배포 (design-p3 §1.3~§1.8·§1.15)

| ID | 확인 | 방법 | 담당 과제 | 판정 |
|---|---|---|---|---|
| V22 | `https://gai-cmd.github.io/interp-app/policy.json` 배포·`no-store` 요청·재조회·설정의 revision 표시 | curl·브라우저 | P3-06·19·35 | 미검증 |
| V23 | 첫 실행 정책 조회 실패 시 시작·진단 차단과 재시도, 실행 중 5분 stale 유지 후 차단, 긴급 중지 배너가 자동 소거되지 않음 | 네트워크 차단 시험 | P3-06·07 | 미검증 |
| V24 | 잠금 아이콘·"관리자가 정한 값"·`aria-describedby` 설명, 색만으로 구분하지 않음 | 스크린리더 | P3-19·20 | 미검증 |
| V25 | `/interp-app/admin/` 직접 접근, 세 언어 부팅, JSON 다운로드·복사·수동 복사 fallback | 브라우저 | P3-32·33 | 미검증 |
| V26 | 관리자 공용 키 payload v2 → 앱 QR 경로 왕복(테스트 키), 닫기·이탈 시 입력 소거 | 브라우저(실키 금지) | P3-34 | 미검증 |
| V27 | 정책·관리자 HTML이 SW 캐시에 없고 정적 JS/CSS만 캐시, 오프라인에서 정책 없음 → 새 실행 차단 | Application 패널·오프라인 전환 | P3-35 | 미검증 |
| V28 | 이전 릴리스 보존과 롤백 후 정책 유지, CDN 반영 지연 관찰 | 배포 루트 확인 | P3-36·39 | 미검증 |
| V29 | 허브 서버 확장 구현 후 협상·snapshot·중지·해제·heartbeat 상실 표시 | 실제 허브 | P3-11 | 미검증 — 서버 미구현 시 fixture까지만 |

## 4. P2 보류 항목 (유지)

아래는 P2에서 실측 증거가 없어 보류된 항목이다. P3의 어떤 자동 검사도 이를 해제하지 않는다. 원본 증거표와 해제 조건은 각 문서에 있다.

| 항목 | 원본 | 현재 판정 | P3 영향 |
|---|---|---|---|
| 실기기·실키 기능 시험(A-W/A-P/I-W/I-P × M0/M1/M2, F01~F12, H01~H07) | [p2-device-results.md](p2-device-results.md) | 미검증 — 입력 대기 | P3의 UI·설정 변경 후 같은 조합을 재시험해야 한다 |
| 공용 직접 Live 모델별 IP 제한 | [p2-device-results.md](p2-device-results.md) | 미검증, 해당 공용 경로 보류 | 행사 정책 `enabled=true`는 이 검증의 증거가 아니다(design-p3 §1.4) |
| 목표 규모·전체 예배 길이(N/L/T, C01~C12) | [p2-capacity-results.md](p2-capacity-results.md) | 합격 보류, 공개 정원 없음 | 변화 없음 |
| PWA 설치·업데이트 실기기 | [release-checklist.md](release-checklist.md) §4 | 미검증 | P3-35·36의 SW 변경 후 재시험 |
| 제공자 이용 조건 확인 | release-checklist §8 | 보류(`terms.status = unreviewed`) | 키 안내 문구가 무료 사용 범위를 단정하지 않아야 한다(P3-03·21) |
| GitHub Pages `_headers` 미적용 | release-checklist | 로컬 검사만 통과 | 정책 `no-store`는 요청 측 구현으로 보완, 실제 헤더는 배포 검증 |
| 허브 운영 계측의 `main.js` 연결 | [p2-review.md](p2-review.md), [p3-00-review.md](p3-00-review.md) | 직접 엔진 계측은 P3-00에서 연결, 허브 계측은 미연결 | P3 범위 밖, 완료로 표시하지 않음 |
| P3-00 연속성·키 저장 재시험(두 모델 비교·잘림·첫소리) | [p2-device-results.md](p2-device-results.md#p3-00-오너-연속성키-저장-재시험) | 오너 시험 대기 | P3-18·22가 자막판·키 UI를 바꾸면 같은 절차로 재시험 |

## 5. 과제 절 추가 양식

P3-12·17·38·39는 아래 양식으로 이 문서 끝에 절을 추가한다.

```markdown
## P3-NN <제목> (YYYY-MM-DD)

- 자동: <실행 명령과 출력 요약>
- 수동 항목 갱신: V## 행에 "구현 완료·미검증" 또는 증거 첨부 판정
- 바꾼 기존 단언: <파일·단언·이유> 또는 없음
- 남은 미검증: <목록>
```

P3-39는 §3 전체를 최종 표로 정리하되, 증거 없는 행은 그대로 `미검증`으로 남긴다.

## P3-12 디자인 토큰·네 가지 톤 (2026-09-06)

- 자동: `node --test tests/design-tokens.test.mjs` 11 통과(실패·취소·skip·todo 0). `node --test tests/*.test.mjs` 734 통과. `node --test tests/` 통과(디렉터리 진입 1). `node scripts/check-i18n.mjs` I18N_OK languages=3 keys=769 files=64. `git diff --check` 통과. Node v24.18.0, HEAD `aa77016` + `styles.css`·`tests/design-tokens.test.mjs`·이 문서 변경.
- 수동 항목 갱신: V01·V02·V05·V07은 P3-12 담당 부분만 "구현 완료·미검증"으로 적었고 나머지 부분은 담당 과제(P3-15·16·17)에 남긴다. V03은 "구현 완료·자동 계산 통과·실측 미검증".
- 바꾼 기존 단언: 없음. `tests/ui-format.test.mjs`의 CSS 문자열 단언(`--touch: 44px`, `.btn {`, `.shell-tab {`, `select, textarea {`, `[hidden]`, px 글자 금지, `url(` 금지)은 그대로 통과한다.
- 남은 미검증: V01(실제 창 크기·스크린샷), V02(개발자 도구 실측, 특히 S 글자와 iOS 확대), V03(대비 도구 실측 — 아래 계산값은 CSS 문자열에서 계산한 것이며 렌더링 실측이 아니다), V05(200% 확대·일본어), V07(키보드 순회·reduced-motion 실기기).

### 계산 대비 (WCAG 2.x 상대 휘도, `tests/design-tokens.test.mjs`와 같은 식)

테스트는 8개 조합 × 21쌍(본문·보조 문구 × 바탕·카드·보조 영역·accent-soft, 악센트 위 글자, 위험 버튼 글자, 텍스트형 버튼의 accent 글자, 상태색 × 바탕·카드)을 모두 4.5:1 이상으로 단언한다. 아래는 그중 대표값과 최소값이다.

| 톤 | 모드 | text/bg | text-muted/surface-alt | accent-text/accent | accent/bg | 21쌍 중 최소 | focus/bg · text/bg |
|---|---|---|---|---|---|---|---|
| navy | light | 15.78 | 6.66 | 6.84 | 6.39 | success/bg 4.99 | 1.92 · 15.78 |
| navy | dark | 15.72 | 6.63 | 8.09 | 8.25 | recording/surface 5.67 | 8.76 · 15.72 |
| warm | light | 15.71 | 6.37 | 6.76 | 6.28 | success/bg 4.97 | 1.91 · 15.71 |
| warm | dark | 15.68 | 6.91 | 7.93 | 8.06 | recording/surface 5.90 | 8.74 · 15.68 |
| forest | light | 15.75 | 6.50 | 6.44 | 6.00 | success/bg 4.98 | 1.91 · 15.75 |
| forest | dark | 15.38 | 6.48 | 9.97 | 9.80 | recording/surface 5.66 | 8.57 · 15.38 |
| mono | light | 16.91 | 6.39 | 18.88 | 18.88 | success/bg 5.35 | 2.05 · 16.91 |
| mono | dark | 18.36 | 8.20 | 21.00 | 21.00 | text-muted/accent-soft 7.13 | 10.23 · 18.36 |

- 포커스 토큰 `#ff9f1c`(DESIGN.md §2 고정값)는 라이트 바탕에서 약 1.9:1이라 단독으로는 WCAG 비텍스트 3:1에 못 미친다. 그래서 `:focus-visible`은 설계대로 2px·offset 2px 주황 outline을 두고, offset 틈에 `--text` 색 2px 링(`box-shadow`)을 더해 어느 바탕에서든 두 링 중 하나가 3:1 이상이 되게 했다(테스트가 bg·surface·surface-alt 각각에 대해 단언). Astra 검수 항목.
- 성공색 `--success`(#1e7a46)는 라이트 바탕에서 4.97~5.35로 여유가 작다. 카드·바탕 위 텍스트로만 쓰고 `--surface-alt` 위 텍스트로는 쓰지 않는다(warm surface-alt 위에서는 4.43으로 미달하므로 테스트도 그 쌍은 요구하지 않는다).
- 다크 모드 `--border`(#3a434d)는 설계 공유 토큰이며 바탕 대비 약 1.8:1의 낮은 구분선이다. 상태는 색이 아니라 텍스트·속성으로 전달하므로 그대로 둔다.

## P3-17 순차통역 반응형 화면 (2026-09-06)

- 자동: `node --test tests/ui-format.test.mjs` 18 통과(기존 14 + P3-17 4, 실패·취소·skip·todo 0). `node --test tests/*.test.mjs` 765 통과. `node --test tests/` 내부 764 + 디렉터리 진입 1 통과. `node scripts/check-i18n.mjs` I18N_OK languages=3 keys=781 files=65. `git diff --check` 통과. Node v24.18.0, HEAD `2f87a71` + `app/ui/seq-view.js`·`styles.css`·`tests/ui-format.test.mjs`·이 문서 변경.
- 수동 항목 갱신: V01·V02·V07의 P3-17 담당 부분을 "구현 완료·미검증"으로 적었다. 실측(430/768/1280px 스크린샷, 개발자 도구 크기, 키보드 순회, iPad 회전 시 1024px 경계 전환)은 오케스트레이터 담당으로 남긴다.
- 바꾼 기존 단언: 없음. `tests/ui-format.test.mjs`의 모의 DOM(`FakeElement.remove`)이 브라우저처럼 분리된 서브트리의 포커스를 잃도록 보강했을 뿐 기존 단언은 그대로 통과한다. `tests/integration.test.mjs`·`tests/app-lifecycle.test.mjs`가 의존하는 말풍선 자식 순서(meta·원문·번역·음성·행동)와 meta의 `[1]` 상태 배지 위치는 유지했다.
- 남은 미검증: V01(2열 실제 배치·긴 번역문 가로 넘침 실측), V02(PTT 64/56px·S 글자·iOS 확대 실측), V07(Tab 순서·전환 시 포커스 유지 실기기·스크린리더), 하단 고정 독이 iOS Safari 키보드 등장·주소창 축소에서도 보이는지(`position: sticky; bottom: 0` + `env(safe-area-inset-bottom)`은 Node에서 검증 불가).

### 구현 요약과 Astra 검수 항목

- 두 배치의 DOM 순서: 적층(<64rem) `언어쌍 → 기록(records 행·빈 안내·log) → 하단 독(상태 행·PTT·시작/종료·취소·힌트·텍스트 폼)`, 데스크톱(≥64rem) `좌 열(언어쌍·독) → 기록`. 뷰가 `document.defaultView.matchMedia('(min-width: 64rem)')`로 실제 노드를 옮기고 `data-layout="stacked|desktop"`을 붙이며, CSS 2열 그리드는 그 속성에만 켜진다. matchMedia가 없으면 적층 순서 그대로다. CSS `order`는 쓰지 않는다(테스트가 금지).
- 하단 독은 `position: sticky; bottom: 0` + safe-area 패딩으로 스크롤·키보드 등장 시 PTT와 입력창을 보이게 하고(DESIGN.md §8), `.shell-main`·`.shell-panel`을 세로 flex로 바꿔 기록이 짧아도 독이 화면 하단(엄지 범위)에 놓이게 했다. **설계와 다른 결정:** P3-12가 넣은 태블릿 `.seq-controls { grid-template-columns: 2fr 1fr 1fr }`(PTT와 시작/종료·취소가 한 행)를 제거하고 모든 폭에서 PTT를 전폭 단독 행으로 두었다. 근거: DESIGN.md §4 "PTT: 전폭". 텍스트 폼은 `textarea | 보내기` 한 행 + 힌트 아래 그리드로 바꾸고 DOM도 `label·textarea·submit·hint` 순으로 맞췄다.
- PTT 문구는 렌더에서 결정한다: 녹음 중 `seq.recording`, 그 외 `seq.holdToTalk`(DESIGN.md §4 "누르는 동안 텍스트 '녹음 중'"). 상태 배지와 말풍선 상태 배지에 `data-state`(recording/error)를 붙여 P3-12의 `.badge[data-state]` 테두리 규칙을 사용한다.
- 말풍선 meta에 엔진(`turn.model`, `mono`)과 경과 시간(`endedAt − createdAt`, `Intl.NumberFormat` unit 초)을 추가했다(DESIGN.md §4 "시각·엔진·지연"). 새 i18n 키는 없다(사전 파일은 이 과제 범위 밖). 경과 시간은 번역 지연이 아니라 턴 시작부터 종료(음성 재생 포함)까지이므로 "지연"의 정의가 다르면 Astra가 판단한다.
- `.turn-text`에 `overflow-wrap: anywhere; max-width: 100%; min-width: 0`, 모든 flex/grid 컨테이너에 `min-width: 0`을 두어 긴 번역문·URL의 가로 넘침을 막았다.

## P3-38 통합·보안·접근성 회귀 (2026-09-06)

- 자동: `node --test tests/p3-integration.test.mjs` 5 통과, `node --test tests/*.test.mjs` 988 통과·실패 0,
  `node scripts/check-i18n.mjs` I18N_OK languages=3 keys=798 files=84, `git diff --check` 통과.
- 검증한 조합: 시트 상호 배타·teardown 후 inert 잔존 없음, 언어 전환이 P3 화면 전체를 따라가며 아무 작업도 시작하지 않음,
  키 교체가 안내·배지·요금제 표시에 동시에 반영, 이번 실행에서 저장소·요청·상태·사용량 관측에 비밀이 새로 흐르지 않음.
- **이 스위트가 증거가 되지 않는 것:** 모의 DOM은 배치를 결정할 수 없다. §3.1의 V01~V09(폭·대비·터치 영역·포커스)는
  이 테스트로 바뀌지 않으며 그대로 미검증이다. 스위트 자체가 이 문서의 그 문장을 단언한다.
- 바꾼 기존 단언: 없음.

## P3-39 오케스트레이터 인계 (2026-09-06)

P3 구현 과제는 모두 착지했다. 아래는 **코드로 확인할 수 없어 사람이 실기기·실배포에서 해야 하는 것**이다.
증거가 없는 칸은 미검증으로 남긴다.

### A. 실기기 화면 (자동 검사 불가)

| 확인 | 방법 | 판정 |
|---|---|---|
| 430 / 768 / 1280px 가로 스크롤 없음, 본문 최대 폭 | 실제 브라우저 창 크기 변경 | 미검증 |
| 네 톤 × 라이트·다크 대비 4.5:1 | 대비 도구 실측 | 미검증 (자동 계산만 통과) |
| XL 글자·일본어·200% 확대에서 헤더·설정 줄바꿈 | 브라우저 확대 | 미검증 |
| 모든 버튼·입력 44px, PTT 64/56px | 개발자 도구 실측 | 미검증 |
| 키보드만으로 전 화면, 포커스 링 2px·순서 = 시각 순서 | 키보드 조작 | 미검증 |
| 시트 3종의 inert·Escape·포커스 복귀·sticky 행동 영역 | 모바일·데스크톱 각각 | 미검증 (자동 회귀는 통과) |
| 첫 페인트 전 표시 설정 적용(깜빡임 없음) | 새로고침 반복 관찰 | 미검증 |

### B. 마이크·오디오 장치 (실기기)

| 확인 | 판정 |
|---|---|
| iOS Safari·Android Chrome·데스크톱 권한 거부 → 안내 → 재허용 | 미검증 |
| 저장 시 임시 트랙 즉시 종료(표시등 꺼짐), 시작 시 이중 요청 없음 | 미검증 (자동으로는 요청 0회를 확인) |
| 권한 후 장치 라벨 표시, 이어폰 연결·해제 갱신 | 미검증 |
| `setSinkId` 지원 브라우저에서 PCM 출력 전환, 미지원에서 미노출 | 미검증 |
| **권한 안내의 메뉴 이름이 실제 지원 버전과 일치** | 미검증 — P3-37이 남긴 항목 |

### C. 배포·정책 (실배포)

| 확인 | 판정 |
|---|---|
| `https://gai-cmd.github.io/interp-app/policy.json` 실제 `no-store` 응답 | 미검증 — **`_headers` 통과는 증거가 아니다** (GitHub Pages는 이 파일을 적용하지 않음) |
| CSP·`no-cache`가 실제 응답에 붙는지 | 미검증 |
| `/interp-app/admin/` 직접 접근, 세 언어 부팅, 다운로드·복사 | 미검증 |
| 관리자 공용 키 payload → 앱 QR 경로 왕복 (테스트 키) | 미검증 — 파서 왕복은 자동으로 통과 |
| 이전 릴리스 보존·롤백 후 정책 유지, CDN 반영 지연 | 미검증 (롤백이 정책을 덮지 않는 것은 자동 통과) |
| PWA 설치·업데이트 실기기 | 미검증 |

### D. 허브 실시간 통제

**서버가 확장을 구현하지 않았다.** 협상·snapshot·중지·해제·heartbeat 상실은 전부 fixture 검증까지만 완료다.
실제 허브 왕복(V29)은 미검증이며, 서버 구현 전에는 "운영 완료"로 표시하지 않는다.

### E. P2에서 넘어온 보류 (그대로 유지)

§4의 항목은 P3의 어떤 자동 검사로도 해제되지 않는다. 특히 실기기·실키 기능 시험,
공용 직접 Live IP 제한, 목표 규모 시험, 제공자 이용 조건 확인은 여전히 입력 대기다.

## P3-40

2026-09-07 최종 검수. **판정: 승인 — 설계·코드 수정·자동 검증 범위. 실키·실기기·배포 검증을 승인했다는 뜻은 아니다.** 기존 변경 `1213f52`는 그대로 승인하지 않고 아래 사항을 수정했다. 사람에게 질문하거나 커밋·배포하지 않았다. 작업 시작 전부터 있던 `tools/`와 `docs/build/` 변경은 이번 과제의 산출물이 아니다.

### 언어 결정

[설계 §1.11.1](design-p3.md#1111-p3-40--동시통역-언어-선택-최종-결정)에 확정했다. 오너 질문의 답은 **“네. 단방향에서는 들리는 말을 선택한 도착 언어로 통역합니다. 이미 그 언어인 말은 읽지 않습니다.”**이다. 발화 명시는 참고 정보이며 타 언어 차단 기능이 아니다. 양방향은 서로 다른 두 실제 언어만 사용하고 자동 선택으로 단방향 요청이 빠져나가지 못하게 했다. 도착=한국어에서 자동 확정되는 상대는 일본어다.

공식 번역 모델 설정에는 `sourceLanguageCode`가 없다. 이 필드를 제거하고 번역 모델의 자막 설정을 `generationConfig` 아래로 옮겼다. 일반 Live의 자막 설정 위치와 양방향 지시문은 유지했다. 발화 언어가 없었다는 사실만으로 영어 오출력의 인과관계를 입증할 수 없다. 지원하지 않는 필드와 잘못된 설정 위치는 시작 실패의 후보이나 당시 실제 응답을 보지 않았으므로 현장 원인으로 단정하지 않는다. 근거: [Google 공식 Live Translate 문서](https://ai.google.dev/gemini-api/docs/live-api/live-translate), 2026-09-07 확인.

### 오류 경로 조사

| 경로 | 확인과 조치 |
|---|---|
| `resolveKey`에 없는 키·코드·잘못된 형태 | 알림·순차·설정·진단·셸·동시통역 호출부를 추적했다. 특정 정상 호출이 항상 잘못된 키를 보내는 사례는 찾지 못했다. 실패 시 최근 식별자 `UNRESOLVED_MESSAGE_KEY`를 진단 화면에 표시한다. 입력 문자열 자체는 보관하지 않는다. |
| `errorKey(PolicyError)` | 기존 제공자 정규화가 정책 코드를 `PROVIDER_ERROR`로 바꾸었다. 정책 오류를 별도 허용 목록으로 보존한다. |
| router → 세션 관리자 → deadline → Live/순차/진단 | 여러 비동기 경계에서 정책 코드가 다시 소실되었다. 정책 오류 정의를 의존성 없는 `app/policy/errors.js`로 분리하고 기존 runtime export는 유지했다. 전달 시 새 안전한 PolicyError만 만들며 정책 차단에는 Live 재연결을 하지 않는다. 진단 결과 코드 허용 목록에도 정책 코드를 포함했다. |
| `keyStoreErrorKey` | 기존 보안 redaction은 정책 오류를 `SECURITY_ERROR`로 바꾸었다. 정책 오류만 별도 매핑하며 다른 보안 오류는 기존 경로를 유지한다. |
| `listenFailure` | 사전에 있는 코드는 동시통역 전용 또는 공통 문구로 나온다. 평범한 Error·미등록 코드는 원문을 버리고 `UNCLASSIFIED_ERROR` 진단 식별자를 남긴다. |
| 보고된 문구 | 한국어의 `error.unknown`과 `error.PROVIDER_ERROR` 문장이 같다. 보고 문장만으로 둘 중 어느 키인지 확정할 수 없다. 최근 진단에는 등록 오류 코드 또는 고정 분류만 남기므로 앞으로 구분할 수 있다. |

진단 식별자는 모듈 메모리의 최근 1개뿐이다. 임의 코드·오류 message·cause·stack·요청·URL·키·발화 원문을 수집하거나 저장하지 않는다. 알 수 없는 오류의 원래 코드를 복원할 수 있다고 주장하지 않는다. 설정 진단을 열거나 갱신하면 최근 분류를 볼 수 있다.

### 직접 시작과 정책 조사

- **권한 거부:** 기존 `ownedListener`는 거부 결과를 null로 바꾸고 `platform.getUserMedia`가 다시 요청했다. 거부를 안전한 브라우저 오류 이름으로 전달하여 캡처가 `MICROPHONE_DENIED`로 끝나게 했다. 실제 조합 테스트로 권한 요청 1회·소켓 0개·원문 노출 없음이 확인됐다.
- **권한 지연:** 캡처의 30초 준비 제한 시간이 적용되며 PCM 첫 프레임 전에는 제공자 연결을 열지 않는다. 취소·시간 초과 뒤 늦게 도착한 스트림은 권한 서비스 또는 캡처가 트랙을 종료한다. 기존 취소·페이지 종료 회귀도 통과했다. 실제 iOS 권한 대화상자의 응답 시간은 측정하지 않았다.
- **입력 장치 병합:** 기존 선행 권한 요청은 `audio:true`여서 캡처의 mono/echo/noise/AGC와 선택 장치를 건너뛰었다. 선행 요청에도 동일한 제약을 `platform.inputConstraints`로 적용한다. `deviceId:{ideal}` 자체는 필수 제약이 아니므로 이 값만으로 Safari 오류를 단정할 근거는 없다. 기본 장치에는 deviceId를 추가하지 않는다. 실제 iOS 장치 전환은 미검증이다. 근거: [W3C Media Capture and Streams](https://www.w3.org/TR/mediacapture-streams/).
- **모델 발견:** 미등록 모델을 자동 채택하지만 `buildLiveSetup`은 등록 모델만 허용하므로 `MODEL_UNSUPPORTED`가 가능한 확정 결함이었다. 자동 채택을 중단하고 실행 전 등록 여부를 검사한다. `liveSetupFor`의 이름 추정은 실제 API 설정 지원의 근거로 사용하지 않는다. 경로 표시도 엔진의 모델 경로 판단과 맞췄다. 모델 검색 응답을 기다리는 것은 시작의 선행 조건이 아니다.
- **정책:** 현재 `policy.json`은 revision 1, `minAppVersion=0.7.0`, 앱도 0.7.0, 긴급 중지 false, 개인 직접 통역 true, 만료 null이다. 정상 수신 시 버전 차단 조건이 아니다. 첫 조회 중/실패, 만료, 긴급 중지, 기능 금지는 의도적으로 차단하고, 공용 키 OFF는 개인 Live를 막지 않는다. 최초 policy.json 배포는 이전 404에서 정상 시작을 가능하게 하는 변화이지 곧바로 차단 원인이라는 증거가 아니다. 실제 폰의 정책 응답·revision 충돌·CDN·서비스워커 상태는 확인하지 않았다.

### 실행 검증과 남은 범위

- `node --test tests/*.test.mjs`: 994개 통과, 실패·취소·skip·todo 0 (기존 988 + 회귀 6).
- `node scripts/check-i18n.mjs`: `I18N_OK`, 3개 언어·800개 키.
- `git diff --check`: 통과.
- `node --test tests/`: 통과(종료 코드 0, 디렉터리 래퍼 1개 통과). 개별 테스트 수의 근거는 위 glob 명령의 994개다.
- 실행 환경: Node `v24.18.0`. 문서·코드 정리 후 위 네 명령을 다시 직접 실행하여 통과를 확인했다.

실기기 V01~V29, 실제 Gemini 개인 키로 번역 방향·음성·자막·연결 확인, iOS Safari의 권한·장치 제약, 모델별 실제 설정 수용 여부, 배포 정책과 gh-pages 반영은 미검증이다. 이번 승인으로 이전 보류 항목을 해제하지 않는다. 새 API·구독·npm 패키지·빌드 도구는 추가하지 않았다.

## P3-41

2026-09-07. **판정: 승인 — 결함 수정·설계 판단·자동 회귀 범위.** 커밋·배포는 하지 않았다.

### 판단과 수정

1. 키 배지는 설정의 키 입력으로 이동하는 버튼을 유지한다. 캡슐 색·테두리·글꼴을 유지하면서 실제 버튼 최소 높이·너비를 44px로 확장했다. 다른 요소를 침범하는 가상 히트 영역은 사용하지 않는다.
2. 개인 단가 입력은 이번 P3에서 제거했다. 세 언어로 정책 단가만 사용한다는 안내를 제공한다. 단일 숫자는 모델·능력·통화를 구분하지 못하므로 정확한 요율 계약 없이 적용하지 않는다. 저장 위치 없음, 숫자·범위·통화 입력 없음, 개인 설정 읽기·쓰기·`usage.setPricing` 호출 없음. 정책 허용 변경은 안내의 표시만 갱신한다. 정책 단가와 구간별 revision 고정은 그대로이며 빈 요율은 0원이 아닌 추정 불가다. §1.13에 범위 결정을 반영했다.
3. 동시통역 언어 컨트롤을 독립 행으로 묶었다. 기존 태블릿 경계인 40rem 이상(검증 폭 768/1280px 포함)은 발화 | 교환 | 도착, 작은 화면은 같은 DOM 순서의 단일 열이다. 교환을 label 밖으로 옮겨 중첩 조작 요소도 제거했다. 허브에서는 발화와 교환을 숨기고 도착 선택을 유지한다. §1.11.1의 언어 의미·양방향 확정 규칙은 바꾸지 않았다.
4. 첫 실행 카드는 유지한다. 직접 통역의 선행 조건인 키 발급·무료 범위 안내를 첫 방문에 읽을 수 있게 하는 선택이다. 430px에서 탭·주 컨트롤이 접힘 아래로 밀리는 사용성 한계는 인정하며, 이번 결함 수정의 차단 조건으로 보지 않는다. 자동 접기·저장 상태는 추가하지 않았다.
5. `sim-open-settings`는 직접 모드의 키 관련 오류 4종과 콜백이 있을 때 표시되고 허브·일반 오류에서는 숨겨진다. 셸은 키 입력을 여는 콜백을 전달한다. 실제 버튼 문구는 `sim.openSettings`로 오류의 “Open settings”와 일치한다. 기존 위치가 상단 컨트롤이라 오류 주변에는 다른 카드 버튼만 보였으므로 오류 문구 바로 다음으로 이동했다. 안내 카드의 “Enter a key in Settings”는 별도 진입점으로 유지한다.
6. 주황 포커스는 DESIGN.md §2의 `--focus: #ff9f1c` 규격과 일치한다. 기존 2px·offset 2px 및 보조 대비 링을 유지한다. 스크린샷의 가시성은 모든 상태의 대비·두께 실측을 대신하지 않는다.

### 실측 증거의 범위

오케스트레이터 제공: 배포본 `p3-40-20260907`, 2026-09-07, headless Chrome, 폭 430/768/1280px, 스크린샷 19장 및 요소 치수 측정. 증거 디렉터리: `/private/tmp/claude-501/-Users-gai/66274a08-d401-46c8-8168-4bb11f20655c/scratchpad/shots/`. 이번 검수에서는 `14-mobile-430-start-nokey-notice.png`를 직접 열어 오류와 키 안내 카드의 인접성을 확인했다. 나머지 측정값은 제공된 실측 보고를 근거로 반영했다.

- V01·V02·V04·V05·V07·V09·V15·V19 행을 부분 확인으로 갱신했다. 이전 과제의 미검증 이력은 보존한다.
- 각 화면 가시 조작 요소 21~65개 중 키 배지만 44px 미달이었다. 이 측정은 수정 전 배포본의 증거이며 수정 후 재측정으로 취급하지 않는다.
- 키 없이 시작하면 `sim.error.CREDENTIAL_REQUIRED` 표시, 양방향에서 발화 ko와 `Korean ↔ Japanese` 조합, 콘솔 예외 0·4xx/5xx 0은 같은 headless 실행의 관측이다. 실제 번역 출력·실키·응답 헤더 적용의 증거가 아니다.
- iOS Safari·Android Chrome 실기기, 실제 키·음성·자막·권한, 실제 응답 헤더, 수정 후 브라우저 치수 재측정은 미검증이다. §4 및 P3-39의 보류 항목을 해제하지 않는다.

### 자동 검증

실행 결과는 아래 최종 검사 기록에 적는다. 기존 `tests/billing-view.test.mjs`의 개인 입력 활성화 단언을 개인 입력 부재·허용 정책 변경 시 안내 표시 단언으로 교체했다. 기능 보류 결정에 맞춘 계약 변경이며 테스트 삭제·skip은 없다. `tests/sim-view.test.mjs`에 언어쌍 DOM 순서·오류 행동 인접성·허브 노출 회귀를 추가했다.

최종 직접 실행 결과:

- `node --test tests/*.test.mjs`: 995개 통과, 실패·취소·skip·todo 0 (기존 994 + 신규 회귀 1).
- `node scripts/check-i18n.mjs`: `I18N_OK languages=3 keys=800 files=85`.
- `git diff --check`: 통과.
- 대상 검사 `node --test tests/billing-view.test.mjs tests/sim-view.test.mjs`: 입력 제거·뷰 수정 후 통과. 이후 추가한 회귀는 위 전체 검사에 포함했다.
- 중간 검사에서 새 48rem 경계가 기존 40/64rem 규약 단언에 걸려 CSS를 40rem으로 수정했다. 신규 허브 전환 테스트는 비동기 전환 완료를 기다리도록 고쳤다. 기존 반응형 단언은 변경하지 않았다.
