P2-08 구현 완료. 지정된 네 파일만 추가했고, 기존 P1 파일 수정이나 커밋은 하지 않았습니다.

- `app/engine/listen-state.js`
- `app/engine/caption-store.js`
- `tests/listen-state.test.mjs`
- `tests/caption-store.test.mjs`

실행 결과:

- 과제 테스트: **12개 통과**
- `node --test tests/*.test.mjs`: **458개 통과**
- `node --test tests/`: **내부 457개와 진입 검사 통과**
- `node scripts/check-i18n.mjs`: **I18N_OK**
- `git diff --check`: **통과**

설계 구체화: 중단된 구간도 최근 종료 구간 100개 제한에 포함했습니다. UI 문구 없이 상태 코드만 제공하며, 사전 연결은 P2-14·16에 남겼습니다.

후속 주의: 허브 이벤트 순서를 보존하고 재접속에서는 epoch를 유지해야 합니다. `newFinal`만으로 낭독하지 말고 목표어·음소거·최근 자막 여부를 확인하세요. 종료 시에는 갱신된 generation으로 정리 완료를 반영해야 합니다.