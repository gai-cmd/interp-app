P2-11을 완료했습니다. 커밋하지 않았습니다.

신규 파일:

- [app/hub/client.js](/Users/gai/work/interp-app/app/hub/client.js)
- [tests/hub-client.test.mjs](/Users/gai/work/interp-app/tests/hub-client.test.mjs)
- [tests/fixtures/hub-socket.mjs](/Users/gai/work/interp-app/tests/fixtures/hub-socket.mjs)

검증 결과:

- `node --test tests/hub-client.test.mjs`: 25개 통과
- `node --test tests/*.test.mjs`: 509개 통과
- `node --test tests/`: 내부 508개와 진입 검사 통과
- `node scripts/check-i18n.mjs`: 통과
- `git diff --check`: 통과

병렬 glob 실행 한 건은 종료 지연으로 중단했고, 단독 재실행은 통과했습니다.

설계 변경은 없습니다. 미정 정책값은 hello·Blob 해석 10초, 종료 확인 5초로 구체화했습니다. 기존 테스트는 수정하지 않았습니다.

후속 과제는 `join()`의 `ready/done/closed`를 사용해야 합니다. **`done` 실패만으로 점유를 해제하지 말고 `closed`로 실제 종료를 확인해야 합니다.** 자막·TTS·재접속 음소거·참가 epoch는 P2-13에서 담당합니다. 실서버·실기기 검증은 수행하지 않았습니다.