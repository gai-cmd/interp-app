P2-12 구현 완료. 지정된 세 파일만 추가했고, 커밋하지 않았습니다.

- [app/audio/caption-speaker.js](/Users/gai/work/interp-app/app/audio/caption-speaker.js)
- [tests/caption-speaker.test.mjs](/Users/gai/work/interp-app/tests/caption-speaker.test.mjs)
- [tests/fixtures/speech.mjs](/Users/gai/work/interp-app/tests/fixtures/speech.mjs)

확정 번역 직렬 재생, revision 재독 방지, 대기 20개 제한, 3초 경고·8초 초과 생략, 음소거·언어 변경·실패 정리를 구현했습니다.

실행 결과:

- `node --test tests/caption-speaker.test.mjs`: 14개 통과
- `node --test tests/*.test.mjs`: 523개 통과
- `node --test tests/`: 내부 522개와 진입 검사 통과
- `node scripts/check-i18n.mjs`: 통과
- `git diff --check`: 통과

실패·skip·todo·취소는 모두 0입니다. 기존 P1 파일과 단언은 변경하지 않았습니다.

설계 대비 구체화한 결정: 삭제된 자막의 수정본도 재독하지 않도록 구간 식별자를 참가 종료까지 보관합니다. 기존 device-tts가 start 이벤트를 노출하지 않아 해당 측정값은 `null`입니다.

다음 과제에서는 같은 epoch의 재접속에 큐 인스턴스를 유지하고, 언어 변경 시 허브 세션도 종료해야 합니다. 식별자 메모리는 참가 길이에 따라 증가하며, 실제 기기 음성 동작은 실기기 검증이 남아 있습니다.