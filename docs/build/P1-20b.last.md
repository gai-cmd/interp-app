완료했습니다. A/B/C, i18n 10개 연결, 스타일, 부팅 방어를 수정했습니다. 빈 기기 음성 목록으로 발생하는 부팅 중단도 모의 환경에서 재현·수정했습니다. 커밋하지 않았습니다.

파일 목록:

- 앱: `app/{main,pwa,state}.js`, `app/i18n/index.js`
- 엔진: `app/engine/{diagnostics,seq,session-manager,voice}.js`
- UI: `app/ui/{errors,seq-view,settings-view,shell}.js`, `styles.css`, `sw.js`
- 테스트: `tests/{app-lifecycle,directory,integration,privacy,settings,state,sw,ui-format}.test.mjs`, `tests/fixtures/scenarios.mjs`, `tests/package.json`
- 문서: [과제별 검수 표·완료 보고서](/Users/gai/work/interp-app/docs/p1-20b-review.md)

검증 결과:

- `node --test tests/*.test.mjs`: **337 통과, fail 0, todo 0**
- `node --test tests/`: 내부 검사 **336 통과**, 진입 검사 통과
- `node scripts/check-i18n.mjs`: **I18N_OK**
- 지정 릴리스 생성·`check-release`: **RELEASE_OK, 52개 파일**, 임시 산출물 정리 완료
- `git diff --check`: 통과

추가 결정은 10초 부팅 제한·영어 JSON 사전 폴백·디렉터리 테스트 진입 파일입니다. 기존 PTT 진단은 독립 STT 검사로 유지했습니다. 실제 Chrome 최초 실패는 **미재현**이며, P1-21에서 실제 PTT·모바일 화면·JSON 모듈 호환성을 확인해야 합니다.

요청한 `rm -rf`는 자동 승인 검토가 강제 삭제 옵션을 이유로 거절해, 생성·검증한 경로를 `rm -r`로 정리했습니다.