P2-03을 완료했습니다. 지정한 5개 파일만 변경했고 커밋하지 않았습니다.

- 신규: `app/providers/gemini/live.js`
- 신규: `app/providers/gemini/live-config.js`
- 신규: `tests/gemini-live.test.mjs`
- 신규: `tests/fixtures/gemini-live.mjs`
- 수정: [docs/reuse-map.md](/Users/gai/work/interp-app/docs/reuse-map.md)

직접 실행한 검증 결과:

- `node --test tests/gemini-live.test.mjs`: **35 통과**
- `node --test tests/*.test.mjs`: **397 통과**
- `node --test tests/`: **내부 396개·진입 검사 통과**
- `node scripts/check-i18n.mjs`: **I18N_OK**
- `git diff --check`: **통과**

실패·skip·todo·취소는 모두 0입니다.

설계 변경은 없습니다. 미지정 세부사항으로 짧은 마지막 PCM 프레임을 허용하고, `finishInput()` 이후 입력 재개는 금지했습니다. 종료 신호는 [공식 프로토콜](https://ai.google.dev/api/live)의 `audioStreamEnd`를 사용합니다.

후속 주의: P2-04는 송신 버퍼 한도, P2-05는 voice/live의 동일 client 주입·등록·복구 정책을 연결해야 합니다. 모델별 실제 지원과 전사 동작은 실키 검증이 남아 있습니다.