P2-16 구현 완료. 지정된 세 파일만 변경했고 커밋하지 않았습니다.

- 신규: `app/ui/sim-view.js`
- 수정: `styles.css`
- 신규: `tests/sim-view.test.mjs`

검증 결과:
- 전용 테스트: **6개 통과**
- `node --test tests/*.test.mjs`: **556개 통과**
- `node --test tests/`: 내부 **555개**, 진입 검사 통과
- `node scripts/check-i18n.mjs`: **I18N_OK**
- `git diff --check`: 통과
- 실패·skip·todo·취소 없음

설계에서 벗어난 변경은 없습니다. 앱 컨텍스트가 필요한 직접 시작은 `startDirect(request)` 주입 인터페이스로 구체화했습니다. 기존 P1 테스트 단언은 수정하지 않았습니다.

다음 P2-17에서는 셸 연결, 사용자 동작 안의 시작 호출, 탭·페이지 이탈 시 엔진 종료를 연결해야 합니다. 화면 `destroy()`는 구독·DOM만 정리합니다. 실제 모바일 확대·스크린리더 검증은 남아 있습니다.