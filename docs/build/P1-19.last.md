P1-19 완료 (Fable). app/main.js(startApp: captureSharedFragment → UI 언어 복원 → loadI18n → manifest 교체 → createAppConfig → fragment 1회 전달 → loadPersonal → getAudioContext(24k, 첫 제스처) → deviceTTS → capture → voiceEngine 1개 → seq 엔진 → mount → diagnostics → pwa → settingsView → pwaControls → register · 종료 순서 · pagehide cancel · 설치 안내 1회), app/pwa.js(createPwa: register('./sw.js') · updateAvailable · applyUpdate: busy→active, clients≠1→other-tabs, 1→apply-update+reload · beforeinstallprompt · iOS 안내 · createPwaControls), tests/app-lifecycle.test.mjs(11, 가짜 브라우저로 실제 모듈 전체 부팅 E2E), tests/pwa-policy.test.mjs(10). 전체 306 통과 · stage-release p1-19 → check-release RELEASE_OK(52 files) · serve 8080 정적 200 확인.
결정: fragment는 holder keyStore로 먼저 정리 후 config 생성 뒤 실제 keyStore에 전달 · pwa.register는 마운트 뒤 비대기 · 버전 조회 3초 타임아웃 · pwa.later 버튼 없음 · localStorage 키 interp-app.ui.v1.language / install-hint.
i18n 갭(오케스트레이터 추가): pwa.closeOtherTabs — app/pwa.js UPDATE_KEYS.otherTabs 연결은 P1-20에서.
주의(P1-20): styles.css에 .pwa-*/.settings-*/.diag-* 규칙 없음 · 통합 테스트 fake fetch는 file: i18n + generativelanguage 둘 다 처리 · 픽스처 sourceText 일치 필요.
스모크(오케스트레이터): serve 8080 → http://127.0.0.1:8080/ → #app .shell, title "통역", lang ko, manifest ko · 설정→키 저장→개인 모드→음성 끔→텍스트 통역(실키 없으면 error.INVALID_KEY 정상)→키 삭제 · #shared= 로드 후 hash 즉시 비는지 · 콘솔 로그 0.
