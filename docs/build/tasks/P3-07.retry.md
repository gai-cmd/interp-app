당신은 Claude Fable 5.1이고, 이 저장소(`~/work/interp-app/`)의 구현자다. 설계·검수는 프로젝트 오너인 GPT-6 Astra가 맡는다 — 설계 문서를 바꾸지 말고 그대로 구현하며, 설계와 어긋나는 결정은 보고서에 근거와 함께 적어 Astra 검수에 넘겨라. 기존 코드의 계약·스타일을 먼저 읽고 맞춰라. 설계 문서는 `docs/design-p3.md`(P3 상세 설계·과제 목록, 우선)와 `docs/DESIGN.md`(디자인 스펙)와 `docs/design-p2.md`와 `docs/design-v0.6.md`(전체 확정 설계)다. P1 구현·검수 보고(`docs/p1-20b-review.md`, `docs/build/P1-*.last.md`)의 계약을 깨지 마라. 아래 **과제 하나만** 구현하라.

규칙:
- 외부 npm 패키지 금지. 빌드 도구 없음. 브라우저 코드는 ES 모듈(상대 경로 import), 테스트는 Node 24 내장 `node --test` (`tests/*.test.mjs`).
- 이 과제에 적힌 파일만 신규·수정한다(테스트·문서 포함). **예외:** 이 과제의 설계 변경으로 P1의 기존 테스트 기대값이 정당하게 무효가 되면(예: 능력 상태 planned→ready), 그 기존 테스트 파일의 해당 단언만 고쳐도 된다 — 무엇을 왜 바꿨는지 보고서에 적고, 테스트를 지우거나 건너뛰지 마라. 다른 과제의 파일은 건드리지 말고, 그 계약이 필요하면 이미 있는 구현을 import해서 쓴다. 없으면 과제 의존 관계에 문제가 있는 것이니 최소한의 인터페이스만 이 과제 파일 안에서 정의하고 보고서에 적어라.
- design-p3.md의 해당 절과 DESIGN.md과 v0.6 §20·§11·§12를 먼저 읽고 그 규약대로 만든다. 재사용 원본 경로: `~/jarvis2/interp-web/lib/translate.js`, `~/jarvis2/interp-web/lib/live.js`, `~/jarvis2/interp-web/lib/xlsx.js`, `~/jarvis2/jp-patch/inject/main-handlers.js`(voiceOpen/voiceSpeak, ttsDirect/voiceDirect), `~/jarvis2/jp-patch/inject/ambient-state.js`(jpGeminiSpeak). 이식한 파일 상단에 출처 주석.
- UI 문자열은 전부 `app/i18n/*.json` 키로(하드코딩 금지). 비밀·키를 로그·오류 객체·URL에 남기지 말 것.
- 코드 주석·커밋 메시지는 영어, 문서(docs/)는 한국어.
- 완료 확인 명령을 **직접 실행해서** 통과시킨 뒤 끝내라. `node --test tests/` 전체도 통과해야 한다(다른 과제의 테스트를 깨뜨리지 말 것).
- git 커밋은 하지 마라(내가 한다). 마지막 메시지에: 만든/고친 파일 목록, 실행한 명령과 결과, 설계와 달리 한 결정, 다음 과제에 넘길 주의점을 한국어로 적어라.

# 과제

## P3-07 — 실행 게이트와 정책 중지 연결

- **목적:** UI 밖 실행 경로까지 정책을 적용한다.
- **만들 파일 6개:** `app/policy/runtime.js`, `app/config.js`, `app/providers/router.js`, `app/main.js`, `tests/policy-runtime.test.mjs`, `tests/app-lifecycle.test.mjs`.
- **의존 과제:** P3-02·06.
- **완료 기준:** 시작·재생·재시도·진단·허브 참가를 검사하고 정책 변경을 기존 정리 경로에 연결한다. 최소 버전·만료·긴급 중지에서 늦은 결과와 자동 재개를 막는다.
- **완료 확인 명령:** `node --test tests/policy-runtime.test.mjs tests/app-lifecycle.test.mjs`; G.
- **예상 함정:** 버튼만 잠금, 종료 timeout 후 Live 점유 해제.


# 이전 시도 실패 · 아래 테스트 출력을 보고 고쳐라
```
ps stream reusable (0.170166ms)
✔ mute clears queued audio and recovery timers; unmute only accepts new audio (2.368791ms)
✔ suspended or interrupted context discards audio with no accumulating buffers or timers (1.010875ms)
✔ resume rejection is recoverable and does not claim physical sound (0.268875ms)
✔ cancel during resume or recovery is terminal, idempotent, and blocks stale callbacks (0.379917ms)
✔ pre-aborted signal and closed context never allocate audio (0.1245ms)
✔ malformed PCM and WebAudio failures are sanitized and release partially built nodes (0.356541ms)
✔ policy overrides cannot exceed hard duration or source bounds (0.269167ms)
✔ registers the four handlers and never skips waiting or claims on its own (25.866041ms)
✔ install precaches the complete shell with cache: reload under a release-tagged cache (1.0815ms)
✔ install fails and removes the partial cache when any shell file is missing, failing or redirected (2.312416ms)
✔ fetch serves shell files cache-first, maps navigation to the cached root and ignores everything else (2.000875ms)
✔ an evicted shell entry falls back to the network without being re-cached (0.827417ms)
✔ activate preserves older shell caches and claims clients only after an explicit update request (1.503833ms)
✔ message protocol answers release and client-count queries and ignores anything else (0.490708ms)
✔ the repository copy is the inert dev release and contains no logging, eval or automatic activation (0.589041ms)
✔ worker rechecks other tabs before applying an update (0.603292ms)
✔ errors.js maps status, phase, error, voice and actions to safe dictionary keys (3.149708ms)
✔ index.html and styles.css are static, i18n-clean and sized for touch and zoom (3.409959ms)
✔ ui sources use only existing dictionary keys and never assign literal UI text (2.7745ms)
✔ mount builds the shell in three languages, shows key badges and manages the settings dialog (4.774417ms)
✔ the simultaneous tab is enabled without starting work; selectTab stays synchronous (1.385083ms)
✔ tab cleanup is awaited, reselection discards late results, and failed cleanup keeps the current panel (1.260542ms)
✔ store notices are displayed, then cleared by the UI through setNotice(null) (0.977584ms)
✔ push-to-talk and the start/finish alternative call the engine synchronously from gestures (5.614875ms)
✔ text input submits from the form, clears on success and turns engine errors into notices (1.664625ms)
✔ bubbles render provider text with textContent and offer retry, play, device re-read and stop (2.010333ms)
✔ the DOM stays bounded to MAX_TURNS during a long session and follows the store order (715.217625ms)
✔ language pair controls keep source and target distinct, swap, and stay independent of the UI language (1.006583ms)
✔ clearing the conversation needs confirmation and is blocked while a turn is active (1.219708ms)
✔ connection badge follows offline events and Live session state; pagehide cancels; destroy detaches (0.735583ms)
✔ before setup and during reconnection input is discarded; ready never replays it (3.908792ms)
✔ eight-frame bound includes pending send, oldest waiting frame is dropped and PCM is copied (1.410041ms)
✔ single pump spaces sends by actual time and never catches up after timer delay (3.609125ms)
✔ stale frames expire at 256ms and a delayed send does not create parallel promises (2.486917ms)
✔ abort discards queue and ignores late send resolution (1.377125ms)
✔ abort discards queue and ignores late send rejection (0.667333ms)
✔ send rejection stops pump and reports only normalized error (0.557291ms)
✔ send throw stops pump and reports only normalized error (0.458583ms)
✔ invalid frames and pre-abort never reach sender; observer exceptions cannot break cancellation (0.457667ms)
✔ rejects invalid composition (0.806334ms)
✔ completed Live turn plays every chunk once, resolves after playback, and reuses the session (3.825292ms)
✔ open failure before audio falls back to device speech once, then cools down and finally suspends (2.345875ms)
✔ fatal errors suspend Live immediately; turn-local errors do not change health (0.301208ms)
✔ failure after the first chunk is partial: no device fallback, no resend, socket closed (0.666833ms)
✔ no audio within the first-audio deadline cancels the turn and falls back to device speech (0.343791ms)
✔ server turn without any audio is a pre-audio failure (0.324916ms)
✔ cancellation stops playback, closes the session, and never falls back (0.791875ms)
✔ a new line cancels the active one, and abort during open closes the opening session (0.780292ms)
✔ device output is explicit, and a missing device voice leaves captions only (0.422875ms)
✔ the session is replaced at a turn boundary when route, language or voice changes (0.719ms)
✔ playback overflow after audio is partial with a gap; blocked playback falls back before audio (0.5055ms)
✔ invalid requests fail without any call; closed engines cancel (0.174416ms)
✔ real stack: router, adapter, live client and player complete and cancel one line each (3.387958ms)
✔ every message key the engine can produce exists in all three dictionaries (2.346167ms)
ℹ tests 710
ℹ suites 0
ℹ pass 709
ℹ fail 1
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 2175.625208

✖ failing tests:

test at tests/policy-runtime.test.mjs:338:1
✖ forced run settings end interpretation, display settings only redraw, pricing and plain revisions are announced, widening reopens (8.532625ms)
  AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:
  + actual - expected
  
  + 'reopened'
  - 'updated'
  
      at TestContext.<anonymous> (file:///private/tmp/interp-lanes/P3-07/tests/policy-runtime.test.mjs:367:10)
      at async Test.run (node:internal/test_runner/test:1332:7)
      at async Test.processPendingSubtests (node:internal/test_runner/test:911:7) {
    generatedMessage: true,
    code: 'ERR_ASSERTION',
    actual: 'reopened',
    expected: 'updated',
    operator: 'strictEqual',
    diff: 'simple'
  }

```
