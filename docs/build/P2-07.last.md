P2-07 구현 완료. 지정한 네 파일만 변경했고 커밋하지 않았습니다.

- `app/audio/stream-player.js`
- `tests/stream-player.test.mjs`
- `tests/fixtures/stream-audio.mjs`
- `docs/reuse-map.md`

검증 결과:

- `node --test tests/stream-player.test.mjs`: 13개 통과
- `node --test tests/*.test.mjs`: 446개 통과
- `node --test tests/`: 내부 445개·진입 검사 통과
- `node scripts/check-i18n.mjs`: 통과
- `git diff --check`: 통과
- 실패·skip·todo·취소 모두 0

설계 목표 변경은 없습니다. 소스 상한 256개, 예약 여유를 포함한 8초 상한, 상태 관찰 주기 50ms를 구체화했습니다. P1 테스트는 변경하지 않았습니다.

후속 과제에서는 `complete → turnComplete()`, `interrupted → interrupt()`를 연결하고, 세대 교체 시 취소해야 합니다. 상태·잘림 알림의 사전/UI 연결은 P2-14·16에 남깁니다. 실제 첫소리는 예약 시각과 구분하여 `null`로 유지하며 실기기 검증은 별도입니다.