P1-13 완료 (Fable). 신규 3개: app/providers/gemini/index.js(GEMINI_DEFINITION·GEMINI_ENDPOINTS·createGeminiAdapter·registerGemini), app/config.js(ENDPOINT_ALLOWLIST/ORIGINS·PRODUCT_PROVIDER_IDS=['gemini']·APP_DEFAULTS·createAppConfig→{registry,keyStore,router,sessionManager,...}), tests/provider-integration.test.mjs(9). 전체 209 통과.
결정: stt에도 3.5-flash 폴백 등록 · 세션 관리자 config에서 1개(60초) — P1-14/16/19는 createVoiceEngine({sessionManager: config.sessionManager}) 주입 · APP_DEFAULTS.interpretation = ko→ja · terms unreviewed.
주의: 라우터는 소비자 close/cancel 뒤 closed 이벤트 없음 · voice 2번째 call은 SESSION_LIMIT → sessionManager.replace() · createAppConfig는 bootstrapSharedKey 뒤 호출 · P1-18 CSP connect-src = ENDPOINT_ORIGINS.
