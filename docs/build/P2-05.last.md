지정된 6개 파일에 구현했습니다. 커밋하지 않았습니다. **전체 테스트 기준은 아직 미충족입니다.**

변경 파일:
- `app/providers/gemini/index.js`
- `app/config.js`
- `app/engine/live-recovery.js` — 신규
- `tests/provider-integration.test.mjs`
- `tests/live-recovery.test.mjs` — 신규
- `docs/architecture.md`

실행 결과:
- 지정 테스트: **19개 통과**
- `node --test tests/*.test.mjs`: 406 통과, **1 실패**
- `node --test tests/`: 내부 405 통과, **1 실패**
- `node scripts/check-i18n.mjs`: 통과
- `git diff --check`: 통과

설계 정책 변경은 없습니다. Live `ready` 등록, voice/live 클라이언트 공유, 별도 Live 폴백, 최초+3회 복구·60초 안정 초기화를 구현했습니다. 후속 엔진은 실행 전체에 복구 객체 하나를 유지하고 실제 종료 확인 후 재연결해야 합니다.

실패 원인은 [tests/settings.test.mjs:291](/Users/gai/work/interp-app/tests/settings.test.mjs:291)의 기존 `live: planned` 기대값입니다. 새 규약에 맞는 실제 상태는 `untested`입니다. 사용자께서 지정한 파일 제한 때문에 수정하지 않았습니다. **이 테스트의 기대값도 수정하도록 범위를 확대해도 될까요?**