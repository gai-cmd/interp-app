P2-02 완료했습니다. 지정된 네 파일만 변경했고 커밋하지 않았습니다.

- 신규: `app/engine/segment-assembler.js`
- 신규: `tests/segment-assembler.test.mjs`
- 신규: `tests/fixtures/segments.mjs`
- 수정: `docs/reuse-map.md`

검증 결과:

- `node --test tests/segment-assembler.test.mjs`: 19개 통과
- `node --test tests/*.test.mjs`: 362개 통과
- `node --test tests/`: 내부 361개·진입 검사 통과
- `node scripts/check-i18n.mjs`: 통과
- `git diff --check`: 통과

실패·취소·skip·todo 모두 0입니다.

설계 구체화: 길이 상한은 140 grapheme, 종료 구간 수정 캐시는 100개로 정했습니다. 모호한 마침표는 다음 입력이나 침묵·턴 종료까지 확정을 보류합니다.

후속 주의: snapshot은 현재 미확정 꼬리를 교체합니다. 제공자의 누적 snapshot은 어댑터에서 정규화해야 합니다. 확정 구간 수정은 `revise()`를 사용하며, 소비자는 높은 revision을 새 음성으로 재생하지 않아야 합니다.