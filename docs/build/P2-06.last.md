P2-06 구현을 완료했습니다. 지정된 5개 파일만 변경했으며 커밋하지 않았습니다.

- `app/audio/stream-capture.js` — 기존 미추적 초안 보완
- `app/audio/uplink-queue.js` — 신규
- `tests/stream-capture.test.mjs` — 신규
- `tests/uplink-queue.test.mjs` — 신규
- `docs/reuse-map.md` — 재사용·인터페이스·인계 기록

검증 결과:

- 지정 개별 테스트: **26개 통과**
- `node --test tests/*.test.mjs`: **433개 통과**
- `node --test tests/`: **내부 432개·진입 검사 통과**
- `node scripts/check-i18n.mjs`: **I18N_OK**
- `git diff --check`: **통과**

실패·skip·todo·취소는 모두 0입니다. P1 테스트 단언은 변경하지 않았습니다.

설계 구체화로 8프레임에 송신 중 1프레임을 포함하고, 256ms 대기 만료·최소 32ms 송신 간격·불완전 꼬리 폐기를 적용했습니다.

P2-09에서는 연결마다 새 큐를 만들고, 재연결 시 이전 큐를 취소한 뒤 소켓 종료를 확인해야 합니다. 입력 누락 표시 연결과 실기기 검증은 후속 과제에 남습니다.