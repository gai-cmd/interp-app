P2-20 완료. 지정된 6개 파일만 수정했으며 커밋·배포는 하지 않았습니다.

수정 파일:

- `app/hub/config.js`
- `app/config.js`
- `_headers`
- `scripts/stage-release.mjs`
- `tests/release.test.mjs`
- `docs/release-checklist.md`

검증 결과:

- `node --test tests/release.test.mjs`: 14개 통과
- `node --test tests/*.test.mjs`: 586개 통과
- `node --test tests/`: 내부 585개·진입 검사 통과
- `node scripts/check-i18n.mjs`: 통과
- `git diff --check`: 통과
- 지정 stage-release·check-release 명령: `p2-check`, 71개 파일 통과  
  OUT: `release/p2-20-check-20260906-1`

설계 조정: 파일 범위와 기존 소비 경로를 보존하도록 허브 등록 목록은 `protocol.js`에서 재사용했습니다. 기존 P1 단언은 변경하지 않았습니다.

후속 주의: 기존 검사기는 origin이 다른 릴리스의 혼재를 거부하므로 허브 주소 추가 시 배포 전환 계획이 필요합니다. GitHub Pages의 실제 CSP 적용 성공은 이번 로컬 검증으로 확인되지 않습니다.