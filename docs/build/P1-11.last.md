지정한 세 파일만 신규 작성했습니다. 커밋하지 않았습니다.

- [live-client.js](/Users/gai/work/interp-app/app/providers/gemini/live-client.js): 브라우저 연결, 순서 보장 디코딩, setup 대기, 종료·시간 초과·goAway·늦은 이벤트 처리.
- [live-client.test.mjs](/Users/gai/work/interp-app/tests/live-client.test.mjs): 회귀 테스트 33개.
- [live.mjs](/Users/gai/work/interp-app/tests/fixtures/live.mjs): 소켓·지연 Blob·타이머 fixture.

실행 결과:

- `node --test tests/live-client.test.mjs`: **33개 통과**
- `node --test tests/`: Node 24.18.0의 디렉터리 처리로 `MODULE_NOT_FOUND` 실패. 기존 `docs/architecture.md`에도 기록된 문제입니다.
- `node --test tests/*.test.mjs`: **전체 171개 통과**
- `git diff --check`: 통과

설계 보완·후속 주의점:

- 미정인 저수준 인터페이스를 파일 내부에 정의했습니다. P1-12는 `open({setup}, context)`와 반환된 `send/close/closed`를 사용하고, 세션 관리자 경유를 유지해야 합니다.
- 종료 확인이 없으면 실패한 `open`을 보류하여 소켓 소유권을 유지합니다. 작업 시간 초과는 기존 세션 관리자가 처리합니다.
- 인증 URL은 설계 §8.1대로 연결 생성 내부에서만 사용합니다.
- 파일 제한으로 `docs/reuse-map.md`는 수정하지 않았습니다. 출처·해시는 구현 상단에 기록했습니다.
- 실제 Gemini 연결·실기기 검증은 수행하지 않았습니다.