P1-20 완료 (Fable). tests/fixtures/scenarios.mjs(가짜 브라우저로 app/main.js 전체 부팅·가상 시계·가짜 마이크/WebAudio/저장소/MessageChannel·스크립트형 Gemini REST/Live), tests/integration.test.mjs(9: 무음·403·PerMinute 429·503 폴백·취소·늦은 응답·목소리 부분 실패·공용 종료·키 교체), tests/privacy.test.mjs(9: fragment 선제거·키는 REST 헤더/Live 소켓 URL에만·개인 키 저장 정책·오류 객체 코드만·출하 소스 정적 검사·CSP·릴리스 스테이징·저장소 키 모양 없음). 324 중 322 통과·todo 2(제품 결함). stage-release p1-review → check-release RELEASE_OK 52.
제품 결함(파일 범위 밖이라 미수정, todo 테스트로 고정): A) UNKNOWN_429가 오류코드 정규식 /^[A-Z_]{1,40}$/(state.js:198, diagnostics.js:33, ui/errors.js:36)에 걸려 INVALID_REQUEST로 표시 → /^[A-Z][A-Z0-9_]{0,39}$/ 권장. B) Live 소켓 비정상 종료(1006) 시 session-manager shutdown이 speak를 ABORTED로 끝내 voice.js가 사용자 취소로 취급 → 기기 음성 폴백/partial 없음 → signal.aborted 아닌 ABORTED를 SESSION_CLOSED로 매핑 권장. C) shell 연결 배지가 voice 엔진 변화를 구독하지 않아 Live 열린 직후 idle.
미처리(파일 범위 밖): i18n 키 연결 10개(seq-view/shell/settings-view/pwa.js), styles.css .settings-*/.diag-*/.pwa-* 규칙.
