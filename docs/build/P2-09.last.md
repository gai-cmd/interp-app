P2-09 구현을 완료했습니다. 커밋하지 않았습니다.

신규 파일:

- [app/engine/sim.js](/Users/gai/work/interp-app/app/engine/sim.js)
- [tests/sim.test.mjs](/Users/gai/work/interp-app/tests/sim.test.mjs)
- [tests/fixtures/sim.mjs](/Users/gai/work/interp-app/tests/fixtures/sim.mjs)

검증 결과:

- `node --test tests/sim.test.mjs`: **17개 통과**
- `node --test tests/*.test.mjs`: **475개 통과**
- `node --test tests/`: **내부 474개·진입 검사 통과**
- `node scripts/check-i18n.mjs`: **I18N_OK**
- `git diff --check`: **통과**

재연결 입력 폐기, goAway 직렬 교체, fatal·중지·권한 지연 취소, 이전 세대 오디오 차단을 검증했습니다. 추가 voice 호출은 없습니다.

설계 구체화: 캡처의 준비 완료 이벤트가 없어 첫 정상 프레임을 준비 신호로 사용하고 폐기합니다. 자막은 기존 어댑터의 조립 결과를 재사용합니다. 파일 제한에 따라 문서와 P1 테스트는 수정하지 않았습니다.

후속 과제는 기존 작업 종료 후 사용자 제스처에서 `start()`를 호출하고, 공통 세션 관리자와 Live 전용 폴백 resolver를 주입해야 합니다. 실키·실기기 검증은 남아 있습니다.