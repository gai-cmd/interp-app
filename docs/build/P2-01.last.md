P2-01 완료했습니다. 허용된 기존 파일 5개만 수정했고 커밋하지 않았습니다.

- `app/providers/contract.js`
- `app/providers/router.js`
- `tests/providers.test.mjs`
- `tests/fixtures/providers.mjs`
- `docs/architecture.md`

`subtitle`의 선택 필드 `segmentId/seq/role`과 `goAway {timeLeftMs}`를 전달합니다. 기존 필드·context ID 우선·임의 필드 제거·종료 후 이벤트 차단을 유지했습니다.

직접 실행한 검증:
- `node --test tests/providers.test.mjs`: 22개 통과
- `node --test tests/*.test.mjs`: 343개 통과
- `node --test tests/`: 내부 342개 및 진입 검사 통과
- `node scripts/check-i18n.mjs`: `I18N_OK`
- `git diff --check`: 통과

실패·skip·todo·취소 모두 0입니다.

설계 이탈이나 의존 인터페이스 대체는 없습니다. 후속 과제는 어댑터에서 구간 필드를 정규화하고, `goAway` 수신 시 입력 중지 → 실제 종료 확인 → 공통 예산에 따른 재연결을 구현해야 합니다.